# ── Authentication (required) ────────────────────────────────────────
# One of these is required. OAuth token is preferred (use `claude login`).
CLAUDE_CODE_OAUTH_TOKEN=
ANTHROPIC_API_KEY=

# ── Assistant identity ───────────────────────────────────────────────
# Display name used in conversations and as the trigger word (@Name).
ASSISTANT_NAME=Hal

# Set to true if the assistant has its own WhatsApp number (not shared with user).
ASSISTANT_HAS_OWN_NUMBER=false

# ── Container settings ──────────────────────────────────────────────
# Docker image to use for agent containers.
# CONTAINER_IMAGE=nanoclaw-agent:latest

# Max time (ms) a container can run before being killed. Default: 30 min.
# CONTAINER_TIMEOUT=1800000

# Max output size (bytes) from a container. Default: 10 MB.
# CONTAINER_MAX_OUTPUT_SIZE=10485760

# How long (ms) to keep a container alive after last result. Default: 30 min.
# IDLE_TIMEOUT=1800000

# Max number of containers running at once. Default: 5.
# MAX_CONCURRENT_CONTAINERS=5


# ── Linear channel ──────────────────────────────────────────────────
# Personal API key (lin_api_...). Create at: Linear > Settings > API > Personal API keys.
LINEAR_API_KEY=

# UUID of the Linear user to watch for issue assignments.
# Find it by running:
#   curl.exe -s -H "Authorization: <API_KEY>" -H "Content-Type: application/json" \
#     -d '{"query":"{ viewer { id displayName } }"}' https://api.linear.app/graphql
LINEAR_USER_ID=

# How often (ms) to poll Linear for new/updated issues. Default: 30 seconds.
LINEAR_POLL_INTERVAL=30000

# 
GITHUB_TOKEN=